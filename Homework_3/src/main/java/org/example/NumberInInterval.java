package org.example;

public class NumberInInterval {

    public boolean numberInInterval(int n) {
        // Проверяем, попадает ли число n в интервал (25; 100)
        return n > 25 && n < 100;
    }
}
